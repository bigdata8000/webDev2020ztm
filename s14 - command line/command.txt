video 14 - command line.

terminal.

ls      = list all files in current dir.
pwd     = present working directory.
cd ..   = change 1 directory up.
clear   = clear screen.
cd /    = go to root directory.
cd ~    = go user directory.
cd <folder/folder/folder> ** <> means to add your own folder names that exist on your computer.

mkdir test  = create dir called name.
open .  = opens the folder.
touch index.html = creates file called index.html in current dir. 
open index.html = 
open -a “Sublime Text” =  

mv index.html about.html    = change name from x to y.

*Try using the Up and Down arrow.

rm <file>
rm -r <folder>


past in terminal:
say this is sooooo cool